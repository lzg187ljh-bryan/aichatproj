# Nginx Dockerfile - 反向代理

FROM nginx:1.25-alpine

# 复制配置
COPY nginx/nginx.conf /etc/nginx/nginx.conf

# 创建静态文件目录（用于错误页面）
RUN mkdir -p /usr/share/nginx/html

# 复制自定义错误页面（可选）
# COPY nginx/50x.html /usr/share/nginx/html/50x.html

# 暴露端口
EXPOSE 80

# 启动 Nginx
CMD ["nginx", "-g", "daemon off;"]
